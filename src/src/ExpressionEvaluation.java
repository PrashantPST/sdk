/*
 * Infix Expressions are harder for Computers to evaluate because of the addional work needed to decide precedence.
 *
 */
public class ExpressionEvaluation {
    public static void main(String[] args) {
        String prefixExpr = "";
        String postfixExpr = "";
        System.out.println(prefixEvaluation(prefixExpr));
        System.out.println(postfixEvaluation(postfixExpr));
    }
    private static int prefixEvaluation(String prefixExpr) {
        return 0;
    }
    private static int postfixEvaluation(String postfixExpr) {
        return 0;
    }
}
